# تعديلات نظام التفعيل والإدارة

تم تعديل المشروع بحيث:

- أي حساب مطعم يسجل بنفسه يبدأ بحالة `pending` وغير مدفوع.
- بعد التسجيل ينتقل المستخدم إلى صفحة `/activation-pending` بدل لوحة التحكم.
- صفحة الانتظار تعرض زر واتساب إلى الرقم `01006923454` برسالة جاهزة بطلب التفعيل وتفاصيل دفع الرسوم.
- المستخدم صاحب المطعم لا يستطيع دخول صفحات لوحة التحكم قبل أن تغيّر الإدارة الحالة إلى `active`.
- لوحة السوبر أدمن تعرض اسم العميل ورقم التواصل وحالة الدفع.
- تمت إضافة زر `اعتماد الدفع وتفعيل` للحسابات المعلقة.
- تم تشديد Firestore Rules لمنع المستخدم من تغيير `role` أو `status` أو بيانات الدفع والتفعيل بنفسه.
- تم تقييد كتابة بيانات المنيو والحساب الإداري على المطاعم المفعّلة فقط (مع بقاء صلاحيات السوبر أدمن).

## مهم عند النشر

1. انشر ملفات الموقع بعد عمل build بالطريقة المعتادة للمشروع.
2. يجب نشر ملف `firestore.rules` الجديد على مشروع Firebase؛ تعديل الواجهة وحده لا يكفي لتطبيق حماية التفعيل.
3. تأكد أن حساب الإدارة في `users/{uid}` لديه `role: "super_admin"`.
4. بعد تسجيل عميل جديد سيظهر في `/admin` بحالة `قيد المراجعة`. بعد استلام الرسوم استخدم زر `اعتماد الدفع وتفعيل`.

## ملاحظة الاختبار

تمت مراجعة التعديلات في المصدر. تعذر إكمال build داخل بيئة العمل الحالية لأن تنزيل حزم التطوير من npm انتهت مهلته، وليس بسبب خطأ TypeScript مؤكد في المشروع. على جهاز التطوير شغّل:

```bash
npm ci
npm run build
```
